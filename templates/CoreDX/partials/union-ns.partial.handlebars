class {{name}} {
  discriminator: {{discriminatorType}};
  {{#each cases}}
  {{name}}?: {{memberType}}
  {{/each}}

  // Factory Static Methods
  {{#each cases}}
  static {{name}}(value: {{memberType}}): {{../name}}
  {{/each}}
}
